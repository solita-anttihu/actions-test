name: Production Install
on:
  release:
    types: [published]
jobs:
  install:
    runs-on: ubuntu-latest
    steps:
      - name: Test
        run: |
          echo env
          echo Testing install
          echo $GITHUB_SHA
          echo $GITHUB_REF
          echo $GITHUB_REF | awk -F/ '{print $NF}'
